apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: opencnapp
spec:
  rules:
    - host: opencnapp.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: opencnapp-api
                port:
                  number: 8000
