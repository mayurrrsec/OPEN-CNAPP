name: kubescape
display_name: Kubescape
domain: cnapp
run_mode: container
enabled: true
schedule: "0 */6 * * *"
image: quay.io/kubescape/kubescape:latest
normalizer: api.adapters.kubescape
config:
  command: ["scan", "framework", "nsa", "--format", "json"]
