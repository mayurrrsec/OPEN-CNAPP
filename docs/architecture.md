# Architecture
OpenCNAPP uses FastAPI + PostgreSQL + Redis + Celery workers and a React dashboard. Plugins declare scanner metadata in YAML, while adapters normalize tool outputs into a unified findings model.
